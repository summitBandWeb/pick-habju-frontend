// 버튼 관련
export enum ButtonVariant {
  Main = 'main',
  Sub = 'sub',
  Text = 'text',
}

// 버튼 Size
export enum BtnSizeVariant {
  XSM = 'xsm',
  SM = 'sm',
  DEFAULT = 'default',
  MD = 'md',
  LG = 'lg',
}

// Chevron 관련
export enum ChevronVariant {
  First = 'first',
  Middle = 'middle',
  Last = 'last',
}

// Time Picker
export enum TimePeriod {
  AM = 'AM',
  PM = 'PM',
}
